# Editable content layout

This is a code bundle for Editable content layout. The original project is available at https://www.figma.com/design/va9XjpvxaG5BQt0BDu0OWZ/Editable-content-layout.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
